import DebtorsApp from '@/components/DebtorsApp';

export const metadata = {
  title: 'Debtors Book — SKM Stores',
  description: 'Track customer debts, purchases and repayments with per-customer ledgers.',
};

export default function DebtorsPage() {
  return <DebtorsApp />;
}
